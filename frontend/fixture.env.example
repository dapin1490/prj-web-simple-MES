# 이 파일을 복사해 `frontend/.env.development.local` 로 저장한 뒤 사용한다.
# (`.env*` 는 gitignore 되므로 커밋되지 않음)

VITE_USE_DEV_FIXTURES=true
# 선택: 픽스처 모드에서 트렌드 타이머 간격(ms)
# VITE_DEV_FIXTURE_TREND_INTERVAL_MS=1500
# 선택: 동일 설비 알림 중복 억제 쿨다운(ms)
# VITE_WS_EQUIPMENT_ALERT_COOLDOWN_MS=10000
